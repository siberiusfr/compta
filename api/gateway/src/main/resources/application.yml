spring:
  application:
    name: api-gateway

  cloud:
    gateway.server.webflux:
      routes:
        # Auth Service Swagger UI
        - id: auth-service-swagger-ui
          uri: http://localhost:8081
          predicates:
            - Path=/auth/swagger-ui/**
          filters:
            - RewritePath=/auth/swagger-ui/(?<segment>.*), /swagger-ui/${segment}

        # Auth Service Swagger Resources (webjars)
        - id: auth-service-swagger-resources
          uri: http://localhost:8081
          predicates:
            - Path=/auth/webjars/**
          filters:
            - RewritePath=/auth/webjars/(?<segment>.*), /webjars/${segment}

        # Auth Service API Docs
        - id: auth-service-api-docs
          uri: http://localhost:8081
          predicates:
            - Path=/auth/v3/api-docs/**
          filters:
            - RewritePath=/auth/v3/api-docs(?<segment>.*), /v3/api-docs${segment}

        # Auth Service Routes
        - id: auth-service
          uri: http://localhost:8081
          predicates:
            - Path=/api/auth/**

        # Accounting Service Routes
        - id: accounting-service
          uri: http://localhost:8082
          predicates:
            - Path=/api/accounting/**
          filters:
            - RewritePath=/api/accounting/(?<segment>.*), /${segment}
            - JwtAuthenticationFilter

        # Document Service Routes
        - id: document-service
          uri: http://document-service:8083
          predicates:
            - Path=/api/documents/**
          filters:
            - RewritePath=/api/documents/(?<segment>.*), /${segment}
            - JwtAuthenticationFilter

        # HR Service Routes
        - id: hr-service
          uri: http://hr-service:8084
          predicates:
            - Path=/api/hr/**
          filters:
            - RewritePath=/api/hr/(?<segment>.*), /${segment}
            - JwtAuthenticationFilter

        # Notification Service Routes
        - id: notification-service
          uri: http://notification-service:3001
          predicates:
            - Path=/api/notifications/**
          filters:
            - RewritePath=/api/notifications/(?<segment>.*), /api/notifications/${segment}
            - JwtAuthenticationFilter

      default-filters:
        - DedupeResponseHeader=Access-Control-Allow-Credentials Access-Control-Allow-Origin

server:
  port: 8080
  shutdown: graceful

# JWT Configuration
jwt:
  secret: ${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
  expiration: ${JWT_EXPIRATION:86400000}

# Management & Actuator
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: always
  prometheus.metrics.export.enabled: true

# Logging
logging:
  level:
    root: INFO
    tn.compta.gateway: DEBUG
    org.springframework.cloud.gateway: DEBUG
    org.springframework.security: DEBUG
  pattern:
    console: '%d{yyyy-MM-dd HH:mm:ss} - %msg%n'
springdoc:
  swagger-ui:
    URLs:
      - url: /v3/api-docs
        name: API Gateway Service
      - url: /auth/v3/api-docs
        name: Auth Service
