# =============================================================================
# Zitadel Environment Variables
# =============================================================================
# Copy this file to .env and fill in the values
# For development: cp .env.example .env
# For production: cp .env.example .env.prod
# =============================================================================

# PostgreSQL Configuration (Development)
POSTGRES_HOST=host.docker.internal
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_SSL_MODE=disable

# Note: Database name is hardcoded to 'zitadel' - Zitadel manages its own schemas

# =============================================================================
# Development Settings (used with docker-compose.dev.yml)
# =============================================================================

# No additional settings needed for dev - defaults are configured in compose file

# =============================================================================
# Production Settings (used with docker-compose.prod.yml)
# =============================================================================

# Zitadel Master Key (REQUIRED - 32 characters)
# Generate with: openssl rand -base64 32 | head -c 32
ZITADEL_MASTERKEY=

# External Domain (REQUIRED - your public domain without https://)
ZITADEL_DOMAIN=auth.example.com
ZITADEL_PORT=443
ZITADEL_LOGIN_PORT=3001

# Organization Name
ZITADEL_ORG_NAME=COMPTA

# Initial Admin User (REQUIRED)
# Login format: <username>@<org_name>.<domain>
# Example: admin@COMPTA.auth.example.com
ZITADEL_ADMIN_USERNAME=admin
ZITADEL_ADMIN_PASSWORD=

# Force password change on first login
ZITADEL_FORCE_PASSWORD_CHANGE=true

# Log level (debug, info, warn, error)
ZITADEL_LOG_LEVEL=warn

# TLS Certificates Path
ZITADEL_CERTS_PATH=./certs

# PostgreSQL Admin (for initial setup)
POSTGRES_ADMIN_USER=postgres
POSTGRES_ADMIN_PASSWORD=

# Zitadel Database User Password
# Generate with: openssl rand -base64 24
ZITADEL_DB_PASSWORD=

# Production PostgreSQL SSL Mode (require, verify-ca, verify-full)
# POSTGRES_SSL_MODE=require
